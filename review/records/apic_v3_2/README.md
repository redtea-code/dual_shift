# APIC v3_2 实验记录

本目录保存 APIC v3_2（protocol revision 4）实验记录，按运行环境分层：

- `5090/`：RTX 5090 上的 MCI vs AD 原型主矩阵、机读表与训练日志。

与 `../apic_v3/`（revision 3）及 `../apis_v2/` 隔离。在 `formal_run_allowed=true` 之前，
本目录中的性能表仅为 **prototype / 非主张** 登记。
